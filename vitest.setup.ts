import { expect } from "vitest";

// Extend expect with custom matchers if needed in the future
// For now, this file ensures consistent test setup

// Mock console.error in tests to keep output clean (optional)
// You can remove this if you want to see console errors in tests
// vi.spyOn(console, 'error').mockImplementation(() => {});
